# Memo Studio 环境变量模板
# 复制此文件为 .env 并修改值

# ⭐ 必须设置：JWT 密钥（建议 32+ 字符）
# 使用命令生成：openssl rand -base64 32
MEMO_JWT_SECRET=

# 推荐：管理员密码（不设置则首次启动随机生成并打印日志）
MEMO_ADMIN_PASSWORD=

# 可选：前端域名（逗号分隔）
MEMO_CORS_ORIGINS=

# 可选：生产环境设为 production
# MEMO_ENV=production
